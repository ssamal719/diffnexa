# Written by hand: a deterministic comparison result, what the scripted model replies,
# and what DiffNexa must then show. See golden/ai-cases/README.md.
case: empty-comparison
description: "An unchanged webpage: an empty comparison is answered without calling any AI service."
tool: web
source: {"pair": "unchanged-page"}
provider: {"error": "failure"}
expect: {"status": "nothing_to_explain", "provider_calls": 0}
